// SPDX-License-Identifier: PMPL-1.0-or-later

= ReScript Evangeliser — EXPLAINME

== "Celebrate good, minimize bad, show better"

[quote, README.adoc]
____
A pattern detection engine, CLI, and educational toolkit that teaches JavaScript developers ReScript through progressive code transformation, without shame.
____

The evangeliser detects 52 patterns across 21 categories in existing JavaScript, then shows equivalent ReScript with encouragement rather than criticism.
It uses progressive disclosure (RAW, FOLDED, GLYPHED views) and Makaton-inspired glyphs to make concepts visual.
See link:README.adoc[] for the full pattern catalogue and quick start.

**Caveat:** The pattern count (52) is the implemented library size in `Patterns.res`. AffineScript evangelism support is early-stage.

=== Evidence

[cols="2,3", options="header"]
|===
| Path | Proves

| `src/Patterns.res`
| 52 pattern definitions across 21 categories -- the core detection library

| `src/Analyser.res` / `src/Analyser.resi`
| Pattern analysis engine with public interface -- takes JS, returns matches

| `src/Narrative.res`
| "You were close!" encouragement narratives -- the shame-free philosophy in code

| `src/Glyphs.res`
| Makaton-inspired glyph system -- visual concept representation

| `src/Scanner.res` / `src/Scanner.resi`
| File/project scanner with public interface -- batch analysis entry point

| `src/Output.res`
| Three output formats (plain, markdown, html) -- progressive disclosure
|===
